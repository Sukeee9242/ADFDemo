# Test Git Environment

This repository contains converted `.py` files from text files.

## 📂 Project Structure
- `*.py` → Python scripts converted from `.txt`

## 🚀 Usage
Clone the repository and run any `.py` file with:
```bash
